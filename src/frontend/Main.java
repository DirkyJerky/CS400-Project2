package frontend;

import data.HTTPClient;

public class Main {

    static HTTPClient httpClient = new HTTPClient();

    // Create the rules for streaming data based on _____
    // Post the rules to the Twitter API
    // Get the tweet stream matching rules
    // Store the tweets in a RBTree

}
